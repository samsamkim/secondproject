<!DOCTYPE html>
<html lang="en">
<head>
  <header>
      <% include ./partials/_header %>
  </header>

</head>
<body>
  <% for (links in urls){%>
    <form method="POST" action= "urls/<%= links %>/delete" >
    <ul>
        <% if (urls[links].userID === cookieid){%>
        <li><%= urls[links].link %> - <%= links %>
           <a href="/urls/<%= links %>"> edit </a>

          <button>delete</button></li>
          <%}%>
    </ul>
    </form>
  <% }%>
  <a href="/urls/new">new url</a>



</body>
</html>
